---
galaxy_info:
  description: helper role that allows to install set of predefined prometheus exporters
  company: http://www.voronenko.info/
  role_name: sa_prometheus_exporters  # if absent directory name hosting role is used instead
  namespace: softasap  # if absent, author is used instead
#  alternate_role_name:
#    - sa-prometheus-exporters
#  standalone: true
  namespace: softasap
  author: vyacheslav voronenko
  description: Installs postgresql, app user, optionally patches listen address and trusted networks
  company: http://www.voronenko.info/
  # If the issue tracker for your role is not on github, uncomment the
  # next line and provide a value
  # issue_tracker_url: http://example.com/issue/tracker
  # Some suggested licenses:
  # - BSD (default)
  # - MIT
  # - GPLv2
  # - GPLv3
  # - Apache
  # - CC-BY
  license: MIT
  min_ansible_version: "2.18"
  #
  # Below are all platforms currently available. Just uncomment
  # the ones that apply to your role. If you don't see your
  # platform on this list, let us know and we'll get it added!
  #
  platforms:
    - name: Ubuntu
      versions:
        - trusty
        - xenial
        - bionic
        - focal
        - jammy
        - noble
  galaxy_tags:
    - database

dependencies: []
  # List your role dependencies here, one per line.
  # Be sure to remove the '[]' above if you add dependencies
  # to this list.
